/*Constructors
Types of constructors:
1.Default Constructor
2.Parameterized Constructor
3.Named Constructor
4.Constant Constructor


/*Abstract Class
--There is no body inside abstract method
--Can access abstract class method through override
--An abstract method can only used in an abstract class

/*Interface
--It uses when all the methods of parent class is mandatory to child class.
--ex: class Television implements Remote
--Can use multiple classes as implements, which is not allowed in extends